package com.booknest.authservice.enums;

/**
 * Enumeration defining the security roles for system access.
 * Controls authorization levels for standard users and administrators.
 */
public enum Role {
	USER, ADMIN
}
