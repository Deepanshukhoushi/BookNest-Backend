package com.booknest.walletservice.entity;

/**
 * Enumeration defining the direction of a wallet transaction.
 * Distinguishes between adding funds (Deposit) and spending funds (Withdraw).
 */
public enum TransactionType {
    DEPOSIT, WITHDRAW
}
